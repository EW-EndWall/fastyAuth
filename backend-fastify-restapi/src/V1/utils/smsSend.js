// * SMS sending function
const sendVerificationPhone = async (request, phone, verificationCode) => {
  // *
};

// * SMS sending function
const sendResetPasswordPhone = async (request, phone, verificationCode) => {
  // *
};

export { sendVerificationPhone, sendResetPasswordPhone };
